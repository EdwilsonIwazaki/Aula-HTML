//Exercício 22
1. No CSS um elemento filho pode ser referenciado por uma separação de espaço.
PAI > FILHO1 > FILHO_DO_FILHO 

R: Sim ex.: PAI FILHO1 FILHO_DO_FILHO 

2. A propriedade CSS background-size permitirá renderizar um tamanho, além de alterar o 
tamanho do arquivo.

R:Errado

3. A tag responsável por criar títulos em até 6 níveis é a HEADER.

R: Não, a tag usada é a H1..H6
